/**
 * Tracks the current pressed state of all keyboard and button inputs.
 * Each property is set to true while the corresponding key is held down.
 */
class Keyboard {
  LEFT = false;
  RIGHT = false;
  UP = false;
  DOWN = false;
  SPACE = false;
  ENTER = false;
}
